// Placeholder mailer. Replace the body with nodemailer/Resend/etc. when you want real emails.
export async function sendMail({ to, subject, text }) {
  console.log(`\n[mail] to=${to}\n[mail] ${subject}\n[mail] ${text}\n`);
}
